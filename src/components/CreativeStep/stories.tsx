import CreativeStep from '.'
import { Meta, StoryFn } from '@storybook/react'

export default {
	title: 'CreativeStep',
	component: CreativeStep
} as Meta

export const Default: StoryFn = () => <CreativeStep />
