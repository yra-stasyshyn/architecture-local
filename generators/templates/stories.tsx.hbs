import {{pascalCase name}} from '.'
import { Meta, Args, StoryFn } from '@storybook/react'

export default {
	title: '{{pascalCase name}}',
	component: {{pascalCase name}},
} as Meta

export const Default: StoryFn = (args: Args) => <{{pascalCase name}} />
